--- a/configure.ac	2010-08-26 11:23:25.000000000 +0200
+++ b/configure.ac	2017-07-16 15:38:07.965028979 +0200
@@ -106,6 +106,22 @@
 )
 AC_SUBST(GTK_CFLAGS)
 
+# GMODULE
+PKG_CHECK_MODULES(GMODULE,
+  gmodule-2.0 >= 2.4,
+  true,
+  AC_MSG_ERROR([need gmodule >= 2.4])
+)
+AC_SUBST(GMODULE_CFLAGS)
+
+# GMODULE
+PKG_CHECK_MODULES(X11,
+  x11 >= 1.0,
+  true,
+  AC_MSG_ERROR([need x11 >= 1.0])
+)
+AC_SUBST(X11_CFLAGS)
+
 # ALSA
 PKG_CHECK_MODULES(ALSA,
   alsa >= 0.9.0,
@@ -444,7 +460,7 @@
 PHASEX_CPPFLAGS="$ALSA_CFLAGS $JACK_CFLAGS $GTK_CFLAGS $SAMPLERATE_CFLAGS -D_GNU_SOURCE -D_REENTRANT -DPHASEX_CPU_POWER=$CPU_POWER_LEVEL $LASH_CFLAGS"
 AC_SUBST(PHASEX_CPPFLAGS)
 
-PHASEX_LIBS="$ALSA_LIBS $JACK_LIBS $GTK_LIBS $SAMPLERATE_LIBS $CONF_LIBS $LASH_LIBS -lpthread"
+PHASEX_LIBS="$ALSA_LIBS $JACK_LIBS $GTK_LIBS $X11_LIBS $GMODULE_LIBS $SAMPLERATE_LIBS $CONF_LIBS $LASH_LIBS -lpthread"
 AC_SUBST(PHASEX_LIBS)
 
 
